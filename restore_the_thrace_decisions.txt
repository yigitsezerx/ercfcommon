restore_the_thrace = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/ep3_cerimonial_decision.dds"
	}
	ai_check_interval = 120

	sort_order = 50

	desc = restore_the_thrace_decision_desc
	selection_tooltip = restore_the_thrace_decision_tooltip
	decision_group_type = roman

	is_shown = {
		has_ep3_dlc_trigger = yes
		OR = {
			has_title = title:e_byzantium
			has_title = title:h_roman_empire
		}
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:restore_the_thrace_decision
			}
		}
	}

	is_valid = {

		completely_controls = title:d_thrace
		completely_controls = title:d_strymon
		completely_controls = title:d_philippopolis
		completely_controls = title:d_turnovo
		completely_controls = title:d_dobrudja
		completely_controls = title:d_bulgaria
		completely_controls = title:d_vidin

		prestige_level >= medium_prestige_level
	}

	is_valid_showing_failures_only = {
		is_at_war = no
		NOT = { exists = involved_activity }
		is_imprisoned = no
		has_contagious_deadly_disease_trigger = no
	}

	cost = {
		gold = 1000
		prestige = 1000
	}

	effect = {
		show_as_tooltip = {
			restore_the_thrace_decision_effect = yes
		}
		trigger_event = {
			id = restore_the_thrace_event.01
			days = 1
		}
		#Can only be done once
		add_to_global_variable_list = {
			name = unavailable_unique_decisions
			target = flag:restore_the_thrace_decision
		}
		if = {
			limit = {
				is_ai = no
				game_start_date >= 1178.10.1
			}
		}
	}

	ai_potential = {
		has_title = title:e_byzantium
		is_at_war = no
		NOT = { exists = involved_activity }
		is_imprisoned = no
		has_contagious_deadly_disease_trigger = no
	}

	ai_will_do = {
		base = 0
	}
}
